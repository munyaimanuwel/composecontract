version: 1
project:
  compose: compose.yml
env:
  example: .env.example
profiles:
  default: []
rules:
  services:
    required: [api]
  env:
    required: [ConnectionStrings__Default, ASPNETCORE_ENVIRONMENT]
    optional: []
  options:
    assemblies: []
    types: []
severity:
  missing_service: error
  missing_env_required: error
  missing_env_optional: warn
  unknown_env_in_example: warn
